# Build artifact — rebuild with package-mod.ps1
LunaMattinsAVCP.zip

# Relay dependencies — restored by npm install / the start launchers
server/node_modules/

# Local machine paths / permissions — never publish
.claude/settings.local.json

# Dev-server scratch output
.claude/shots/
.claude/take.json

# OS cruft
Thumbs.db
.DS_Store
